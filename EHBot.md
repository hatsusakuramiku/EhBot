# EHBot 重构需求说明书（v2）

> **文档性质**：重构需求基线。取代此前的 v1 需求稿。
> **重构范围**：UI 设计 + 流程控制（信息架构、实时机制、交互组件、路由分层）。
> **不在范围**：已验证的后端能力（来源链、归档流水线、安全门禁、标签翻译、幂等契约）。
> **依据**：`COMPETITIVE_ANALYSIS.md`（11 个同类项目调研 + 现状诊断）。

## 0. 为什么重构

当前实现的后端能力已经超过多数同类项目，但交互层停留在「能用」：

- 「实时进度」是 `<meta http-equiv="refresh">` **整页刷新**——同类项目无一例外都是局部更新。
- 全站**没有一张封面图**，而 Kavita/Komga/LANraragi 的主界面都是封面网格。
- 导航**平铺 9 项**，候选状态散落 4 个页面、设置散落 4 个页面，而 *arr 系用「Activity/Library/Settings + Tab」聚合。
- 仅有 **1 个 JSON API**、**16 行 JavaScript**，前端无局部更新能力。
- `app/main.py` 单文件 **88KB / 59 路由**，UI 与业务耦合，无法在其上做交互演进。

因此：**保留后端资产，重写交互层，重组信息架构与流程控制。**

## 1. 重构目标

| 编号 | 目标 | 验收信号 |
|------|------|----------|
| G1 | 封面驱动的视觉体系 | 候选/书库主视图为封面网格，支持网格/列表切换 |
| G2 | 无整页刷新 | 全站无 `meta refresh`；进度经 JSON 轮询 + SSE 局部更新 |
| G3 | 信息架构收敛 | 顶级导航从 9 项收敛为 5 域，同类任务在域内以 Tab 组织 |
| G4 | 完整 JSON API | 每个页面数据均有对应 `/api/v1/*`，页面壳与数据解耦 |
| G5 | 流程可视化 | 单个作品从消息到入库的全链路状态在一处可见 |
| G6 | 路由分层 | `main.py` 拆为 `app/api/` + `app/web/`，单文件 < 500 行 |
| G7 | 书库纳管 | CBZ 落盘后进入书库页，可浏览、搜索、重打包、修改路径 |
| G8 | 后端零回归 | 现有 439 项测试全绿，安全门禁与幂等契约不变 |

## 2. 目标信息架构

从 9 个平铺导航收敛为 **5 个域**：

```text
① 工作台 /                    概览、待办、连接健康度、吞吐趋势
② 候选   /candidates          [全部|待审核|待补充|已通过|驳回|失败]  ← Tab 聚合，取代 4 个独立页
③ 活动   /activity            [队列|打包|历史]                      ← 取代 下载任务 + 历史 + 打包区块
④ 书库   /library             封面网格、搜索、筛选、详情、重打包     ← 新建
⑤ 设置   /settings            [连接|来源规则|自动审批|归档|路径|密码库|系统] ← Tab 聚合，取代 4 个页面
```

作品详情 `/works/{id}` 作为**贯穿全流程的统一详情页**：无论作品处于候选、下载中、已入库，都是同一个页面，仅底部操作区与状态时间线随阶段变化。

## 3. UI 设计需求

### 3.1 设计语言

| 项 | 要求 |
|----|------|
| 主题 | 深色为默认，浅色可切换，跟随系统偏好 |
| 布局 | 桌面左侧固定侧栏（可折叠为图标栏）；移动端底部 Tab Bar + 抽屉，**不再维护两套导航结构** |
| 密度 | 支持「舒适/紧凑」两档，列表类页面记忆用户选择 |
| 响应式 | 断点 640 / 1024 / 1440；封面网格列数自适应 |
| 无障碍 | 键盘可达、焦点可见、`aria-live` 播报异步结果、对比度 ≥ 4.5:1 |

### 3.2 必备通用组件

| 组件 | 用途 | 现状 |
|------|------|------|
| 封面卡片 | 候选/书库主视图单元，含封面、标题、作者、标签、状态徽章 | 🚧 新建 |
| 状态徽章 | 统一映射状态枚举 → 中文标签 + 语义色 | 🚧 现为原始英文枚举 |
| 数据表格 | 排序、列筛选、分页、密度切换 | 🚧 现为 div 模拟表格 |
| 批量工具条 | 多选后浮现，含全选/反选/计数与批量动作 | 🚧 仅候选页有裸 checkbox |
| Toast 通知 | 异步动作结果反馈，不打断页面 | 🚧 现靠整页跳转 |
| 确认对话框 | 破坏性操作（删文件、移除任务）二次确认 | 🚧 缺失 |
| 进度条 | 下载/打包进度，局部更新 | ⚠️ 有 `<progress>` 但靠整页刷新 |
| 抽屉/弹层 | 元数据编辑、规则编辑，不离开列表上下文 | 🚧 缺失 |
| 筛选侧栏 | 按标签/作者/语言/分类/状态多条件筛选 | 🚧 缺失 |
| 空状态 | 每个列表的引导性空态 | ✅ 已有雏形 |
| 骨架屏 | 首屏与切换时的加载占位 | 🚧 缺失 |

### 3.3 封面缩略图（新增能力）

- 候选阶段：使用 gdata 已解析的 `thumb` URL，经**服务端代理缓存**（不让浏览器直连第三方图床，避免防盗链与隐私泄露）。
- 入库阶段：从 CBZ 首图抽取，生成 WebP 缩略图（长边 512 / 256 两档）。
- 缓存位置 `data/thumbnails/{sha256前2位}/{sha256}.webp`，带 `ETag` 与长缓存头。
- 缺失时回退占位图，不阻塞页面。

## 4. 流程控制需求

### 4.1 实时机制（取代整页刷新）

分三级，按成本递增：

| 级别 | 机制 | 适用 |
|------|------|------|
| L1 | JSON 轮询，可见时 2s / 隐藏时暂停（`visibilitychange`） | 队列进度、打包进度 |
| L2 | SSE `/api/v1/events`，服务端在状态跃迁时推送 | 任务完成、失败、需要干预 |
| L3 | 乐观更新 + 失败回滚 | 审核通过/驳回等即时动作 |

要求：**轮询期间若后端完成，SSE 立即推送并强制刷新该行状态**（原始需求即为此意图，此前用整页刷新近似实现）。

### 4.2 统一作品生命周期

单一状态时间线贯穿全流程，在 `/works/{id}` 一处呈现：

```text
发现 → 元数据就绪 → 待审核 → 已通过 → 下载中 → 已下载 → 打包中 → 已入库
                        ↓          ↓        ↓                ↓
                    待补充/驳回   失败    等待种子/缺分卷/缺密码
```

每个节点展示：时间戳、执行者（自动规则 / 操作员 / 系统）、可回溯的原因说明、可用动作。

### 4.3 队列控制

- 队列按状态分组（进行中 / 等待中 / 需干预 / 已暂停），支持批量暂停、恢复、取消、重试、切换来源。
- 需干预项（缺分卷、缺密码、预览页缺页、种子停滞）**在工作台与队列顶部聚合提示**，不埋在列表里。
- 支持拖拽或显式优先级调整（🚧 新增，同类项目普遍具备）。
- 并发上限可在设置中调整，队列页显示当前占用/上限。

### 4.4 审核流程

- 候选页支持：多选 → 批量通过/驳回/待补充；行内快速通过；键盘快捷键（`j/k` 移动、`a` 通过、`x` 驳回）。
- 元数据编辑改为**抽屉内就地编辑**，字段显示来源标注（gdata / 翻译 / 手动），支持**字段锁定**防止后续刮削覆盖。
- 自动审批规则编辑器（参考 autobrr）：条件分组、字段下拉、实时语法校验、**对历史候选试跑并显示命中数与命中样例**。

### 4.5 书库纳管（新增）

- 封面网格 + 搜索 + 多条件筛选 + 排序。
- 详情页可：重新打包（可选用原打包元数据或重新解析）、修改归档路径与文件名、重新下载并替换、删除（可选是否删文件）。
- 归档路径支持模板：`{category}/{artist}/{title}`，模板变量取自元数据，保存前预览渲染结果。
- 扫描已有目录导入（🚧 可选增强）。

## 5. 后端配套需求

### 5.1 JSON API（新增 `app/api/`）

| 路由 | 说明 | 状态 |
|------|------|------|
| `GET /api/v1/meta` | 状态词表、功能开关、轮询间隔 | ✅ R0 |
| `GET /api/v1/events` | SSE 状态推送 | ✅ R0 |
| `GET /api/v1/summary` | 工作台聚合指标 | ✅ R1 |
| `GET /api/v1/candidates` | 分页/筛选/排序候选列表（含 tab 计数） | ✅ R1 |
| `GET /api/v1/works/{id}` | 作品详情 + 元数据 + 时间线 + 关联任务 | ✅ R1 |
| `PATCH /api/v1/works/{id}/metadata` | 字段级更新（锁定待 R2 迁移） | ✅ R1 |
| `POST /api/v1/candidates/batch` | 批量审核 | ✅ R1 |
| `GET /api/v1/queue` | 队列快照（含进度、停滞、是否仍在推进） | ✅ R1 |
| `GET /api/v1/history` | 已结束任务 | ✅ R1 |
| `POST /api/v1/jobs/{id}/{action}` | pause/resume/cancel/retry/stop-seeding | ✅ R1 |
| `POST /api/v1/jobs/{id}/switch-source` | 切换下载来源 | ✅ R1 |
| `GET /api/v1/thumbnails/{hash}` | 缩略图（带缓存头） | R2 |
| `GET /api/v1/library` | 书库列表 | R7 |
| `POST /api/v1/library/{id}/repack` | 重新打包 | R7 |
| `GET/PUT /api/v1/settings/{section}` | 分区设置读写 | R8 |

约定：统一错误体 `{code, message, details}`；分页 `{items, total, page, page_size}`；写操作需 CSRF 或同源校验。

### 5.2 路由分层（拆 `main.py`）

```text
app/api/        JSON 路由，按域分模块（summary/candidates/works/queue/library/settings/events）
app/web/routes/ 页面壳路由，仅渲染模板与注入初始状态
app/main.py     仅保留 create_app、lifespan、依赖装配（目标 < 500 行）
```

### 5.3 数据模型增量

| 变更 | 说明 |
|------|------|
| `thumbnails` 表 | 缩略图缓存索引（source_url / sha256 / path / size） |
| `library_items` 表 | 入库条目（candidate_id / cbz_path / library_path / packed_at / page_count） |
| `metadata_values.is_locked` | 字段锁定标记 |
| `download_jobs.priority` | 队列优先级 |
| `archive_settings` 新键 | 归档路径模板、并发上限、主题偏好 |

**约束**：全部为新增表/新增列，不做破坏性迁移；现有 11 个迁移文件不改，从 `012_*` 起追加。

### 5.4 打包任务与下载任务解耦

当前把转换任务以 `provider='CONVERSION'` 塞进 `download_jobs`，导致队列语义混乱（F5）。重构后：
- 新增 `conversion_jobs` 表承载打包任务，或在 API 层清晰分流为两个队列视图。
- 活动页以「队列 / 打包 / 历史」三 Tab 分别呈现，不再混在一个列表。

## 6. 业务边界（继承 v1，不变）

### 6.1 继续做

Telegram 监听与消息摄取、消息归并、ExHentai 元数据与中文标签翻译、候选审核、正则自动审批、来源降级链下载、归档安全流水线、CBZ + `ComicInfo.xml` 发布、Docker 单容器部署。

### 6.2 明确不做（⛔）

- 不引入 MTProto/Telethon（Bot API 20MB 上限即转种子/预览页）。
- 不逐页抓取 EH 画廊本体；仅抓频道自建 telegra.ph 预览页作兜底，有张数/字节/超时上限。
- 不做 OCR、翻译、去水印、超分。
- 不做多租户、公开注册、复杂 RBAC（单管理员）。
- 不引入 Redis/Celery/PostgreSQL/消息队列集群。
- 不提供公网 HTTPS 证书自动签发；TLS/反代由部署者负责。
- 不绕过 Telegram/ExHentai 权限与风控。
- 不处理 `t.me/c/...` 频道消息链接。
- **重构新增排除**：不引入重量级前端框架构建链（保持无 Node 运行时依赖，见 §7）。

### 6.3 不可破坏的既有契约

- 审核前不下载完整文件；仅 `APPROVED`/`DOWNLOADED` 可入队。
- 元数据以 ExHentai 为唯一权威源。
- 幂等：`idempotency_key` + 唯一约束；重启恢复；重试复用同一任务行。
- 画质优先、成本其次；预览页是兜底而非默认；Archive Download 永远手动。
- 种子停滞不是失败，交由操作员决策。
- 打包是显式决策（`auto_pack_after_download` / `torrent_auto_pack` 默认关）。
- 凭据不落明文、页面不回显、日志不记录。
- 安全门禁（路径穿越、压缩炸弹、SSRF、图片魔数）不得放宽。

## 7. 技术选型（前端）

延续「无 Node 运行时」的部署约束，同时获得局部更新能力：

| 方案 | 结论 |
|------|------|
| **Jinja2 + HTMX + Alpine.js（推荐）** | 服务端渲染保留，HTMX 做局部替换与 SSE，Alpine 管理轻量交互态；静态资源随镜像分发，无构建链 |
| React/Vue SPA | 能力最强，但引入 Node 构建链与前后端契约维护成本，违背单容器轻量目标 |
| 维持现状（纯表单 + 整页刷新） | 无法达成 G1/G2/G5，排除 |

HTMX 与 Alpine 以固定版本的静态文件随镜像分发（不依赖 CDN，离线部署可用）。

## 8. 验收标准

1. 全站无 `<meta http-equiv="refresh">`；队列进度经轮询/SSE 局部更新，页面不跳动。
2. 候选与书库主视图为封面网格，缩略图经服务端代理与缓存，首屏有骨架屏。
3. 顶级导航为 5 项；候选 6 个状态与设置 7 个分区均以 Tab 组织在域内。
4. `/works/{id}` 呈现完整状态时间线，含时间戳、执行者与原因。
5. 每个页面数据均有对应 `/api/v1/*`，返回统一错误体与分页结构。
6. `app/main.py` < 500 行；API 与页面路由分层。
7. 书库页可浏览、搜索、筛选、重打包、改路径、删除（可选删文件）；路径模板保存前可预览。
8. 批量操作具备全选/反选/计数与浮动工具条；破坏性操作有二次确认。
9. 自动审批规则可对历史候选试跑，显示命中数与样例。
10. 键盘可达、`aria-live` 播报、对比度合规；移动端单套导航结构。
11. 现有 439 项测试全绿；新增 API 与前端交互有对应测试。
12. 安全门禁与幂等契约行为不变，回归测试覆盖。

## 9. 关联文档

| 文档 | 作用 |
|------|------|
| `COMPETITIVE_ANALYSIS.md` | 同类项目调研、现状诊断、重构依据 |
| `DEVELOPMENT_PLAN.md` | 重构分阶段开发计划与工作量 |
| `README.md` | 使用与部署手册（重构完成后同步更新） |
| `task_plan.md` / `progress.md` | 实施进度记录 |
| `ARCHIVE_PROCESSING_PROPOSAL.md` 等 4 份 | 既有后端设计记录，重构中作为契约参考 |