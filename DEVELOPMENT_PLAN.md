# EhBot 重构开发计划（v2）

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | 2.0（重构计划，取代 v1 从零建设方案） |
| 编制日期 | 2026-08-25 |
| 项目性质 | **存量重构**（UI + 流程控制），非从零建设 |
| 需求基线 | `EHBot.md` v2 |
| 调研依据 | `COMPETITIVE_ANALYSIS.md` |
| 部署形态 | Docker 单容器（不变） |
| 前端选型 | Jinja2 + HTMX + Alpine.js（无 Node 运行时） |

## 2. 重构原则

| 原则 | 含义 |
|------|------|
| **后端资产不动** | `app/archive`、`app/exhentai`、`app/torrent`、`app/telegraph`、`app/conversion`、`app/downloads`、`app/candidates` 的领域逻辑保持不变，仅新增读取接口 |
| **迁移只追加** | 从 `012_*` 起新增，不修改既有 11 个迁移，不做破坏性变更 |
| **API 先行** | 每个页面重写前先落 JSON API 并测试，页面只消费 API |
| **绿灯前进** | 每阶段结束时既有测试必须全绿（基线随阶段推进：R0 439 → R1 524 → R2 569 → … → R9 820 → R10 939），新增能力必须带测试 |
| **可回退** | 新旧页面在过渡期并存（新路由 `/v2/*` 或特性开关），验证通过后再切换默认并移除旧模板 |
| **不扩范围** | 严格遵守 `EHBot.md` §6.2 排除项；重构不顺带加新业务功能 |

## 3. 阶段划分

### 阶段 R0：基线固化与脚手架（1～2 人日） ✅ 已完成

> 完成于 2026-08-25。测试：439 → **481 passed / 0 failed**（+42，无回归）。
> 实施记录与决策理由见 `progress.md` 的「Phase R0」小节。

内容：
- 运行全量测试并记录基线（当前 439 passed / 0 failed）。
- 引入固定版本 HTMX、Alpine.js 静态文件到 `app/web/static/vendor/`（离线可用，不依赖 CDN）。
- 建立 `app/api/` 包与 `app/web/routes/` 包骨架，接入 `create_app`。
- 定义统一响应契约：`ApiError{code,message,details}`、`Page{items,total,page,page_size}`。
- 建立前端约定：CSS 设计令牌（颜色/间距/圆角/密度）、组件命名规范。

验收：✅ 脚手架就位；`/api/v1/meta`、`/api/v1/events`、`/api/v1/events/stats` 可访问且鉴权生效；设计令牌与 vendor 资源均正常供给。

### 阶段 R1：JSON API 层与共享审核编排（4～5 人日）✅ 已完成

内容：
- 按域实现只读 API：`summary`、`candidates`、`works/{id}`、`queue`、`history`。
- 实现写 API：批量审核、任务动作（pause/resume/cancel/retry/stop-seeding/switch-source）、元数据 PATCH。
- 抽出 `ReviewOrchestrator`：选源与「审核→入队」不再是 `create_app` 闭包，页面与 API 走同一条路径。
- SSE 接入下载 worker：在 `_process_one` 单点推送（而非逐个终态分支）。

验收：524 passed / 0 failed（+43，零回归）；契约与写门禁测试齐备；旧页面行为不变。

> **计划变更**：`main.py` 拆分不再在本阶段一次完成。元数据/API/编排已抽出（2180 → 2062 行），但 59 个旧 HTML 路由必须等取代它们的新页面存在后再搬，否则每个路由要迁移两次。改为随 R4～R8 按域搬迁，R9 收口并验收 < 500 行。
>
> `settings/{section}` 只读端点顺延至 R8（依赖其分区划分）。同时顺延的 `library` 端点没有以那个名字落地：R7 于 2026-08-26 删除，2026-08-28 恢复为范围更窄的已下载域，端点是 `GET /api/v1/downloaded`（R10）。

### 阶段 R2：缩略图服务与数据模型增量（2～3 人日）✅ 已完成

内容：
- 迁移 `012_thumbnails_locking_priority.sql`：`thumbnails` 表，`candidates.thumb_url`，`metadata_values.is_locked`，`download_jobs.priority`，`artifacts.page_count`。
- 候选缩略图：服务端代理 gdata `thumb`，走既有 SSRF 门禁与图片魔数校验，统一重编码为 WebP 落 `data/thumbnails/`。
- `GET /api/v1/thumbnails/{hash}`：`ETag` + `immutable` 长缓存 + 失败回退占位图（200 而非 404）。
- 三个新列当期即接上读取方：`is_locked` 进入刮削写入门禁与 `PATCH /works/{id}/metadata` 的 `locks`，`priority` 进入下载队列取任务排序。

验收：✅ 569 passed / 12 skipped / 0 failed（+45，零回归）。缩略图二次请求命中磁盘且 `If-None-Match` 换 `304`；防盗链 403 带 `Referer` 重试一次即成功；上游用 200 送回 HTML 错误页被魔数门禁挡下；私网地址在抓取前即被拒；接口不接受任何调用方给的 URL。

> **范围收窄（2026-08-26，依操作者指示）**：本项目只负责「下载 → 转成目标归档格式」及少量便于操作的管理项，详细的图书管理交给下游工具。因此 R2 去掉了 `library_items` 表与「CBZ 首图缩略图」，原 **阶段 R7（书库域）整节删除**；其中唯一仍需落地的归档路径模板 `{category}/{artist}/{title}` 并入 R8。已打包的 CBZ 只在活动域历史里可见（复用既有 `artifact_cbz_path`），不新增登记表。
>
> **部分恢复（2026-08-28，依操作者指示）**：已下载内容需要勾选 + 批量操作，因此按 `EHBot.md` §1.3.1 恢复为 **R10（已下载域）**。恢复的边界与原 R7 不同：**没有**恢复 `library_items` 表（已下载域直接读 `download_jobs` + `artifacts`，另建登记表就有两份真相），**没有**恢复 CBZ 首图缩略图（复用候选阶段封面），也没有阅读器与导入扫描。恢复的只有清单、批量三动作与单件改名/改路径。
>
> **顺带修掉的既有缺陷**：`_record_cbz_artifact_sync` 过去把 `page_count` 写进了 `artifacts.size_bytes` 且不写 `sha256`，于是每个打包成品都报告几十字节的体积。现在三列各归其位，`page_count` 用迁移 012 新增的列承载。

### 阶段 R3：设计系统与通用组件（3～4 人日）✅ 已完成

内容：
- 重写 `app.css` 为设计令牌 + 组件层：深/浅主题、舒适/紧凑密度、断点 640/1024/1440。
- 实现通用组件：封面卡片、状态徽章（枚举→中文+语义色）、数据表格（排序/筛选/分页/密度）、批量工具条、Toast、确认对话框、抽屉、筛选侧栏、骨架屏、进度条。
- 统一导航壳：桌面可折叠侧栏 + 移动端底部 Tab Bar + 抽屉，**单套导航数据源**（消除 `base.html` 双导航）。

验收：✅ 592 passed / 12 skipped / 0 failed（+23，零回归）。`/ui-kit` 演示页可交互；`skip-link` + 单一 `aria-current="page"` + `aria-live` 区域随文档一同存在；抽屉与对话框 Esc 可关且焦点入内；两套导航渲染自同一份 `NAV_ITEMS`，测试逐条比对其目标集合。

> **实现说明**：新样式落在 `ui.css`，`app.css` 冻结为改造前的页面规则，R9 直接 `rm`——文件边界不会像注释边界那样被切错。因此 `ui.css` 的规则全部按 class 限定：`app.css` 仍硬写着浅色 `body` 背景与浅色 `--ink`，若在此处写裸 `body` 规则，13 个未改造页面在深色下会变成约 3.2:1 的灰字，正好违反本阶段自己的 ≥ 4.5:1。未改造页面由 `.ui-main[data-legacy="true"]` 锁在浅色，逐页在 R4～R8 摘掉。
>
> 导航树保留两级（域 + 分区）而非计划里的扁平四项：`设置` 分区要到 R8 才成形，而 `来源规则`/`自动审批`/`归档设置`/`外部连接` 现在都是活页面，扁平四项会让其中 6 个页面无法抵达——那是拿真实回归换与计划字面一致。R8 收拢分区时改一处即可。
>
> **顺带修掉的既有缺陷**：旧 `base.html` 的两套导航是手写的，移动端多出一个侧栏从未获得的 `历史` 链接。另有一处此次新写代码中被测试当场拦下的缺陷：`/downloads` 是 `/downloads/history` 的前缀，于是父项与子项会同时输出 `aria-current="page"`，屏幕阅读器会报出两个「当前页」；`NavItem.is_current()` 现在只让叶子节点认领，索引子项（`/candidates`、`/downloads`、`/connections`）改为精确匹配。

### 阶段 R4：活动域（队列/打包/历史）（3～4 人日）✅ 已完成

内容：
- `/activity` 三 Tab：队列、打包、历史。
- 移除 `<meta http-equiv="refresh">`，改为可见时 2s 轮询 + 隐藏时暂停 + SSE 完成即推。
- 队列按「进行中/等待中/需干预/已暂停」分组；批量暂停/恢复/取消/重试/切来源。
- 需干预项（缺分卷、缺密码、预览页缺页、种子停滞）在页顶与工作台聚合提示。
- 打包任务与下载任务在视图与 API 上分流（解决 `provider='CONVERSION'` 混淆）。
- 队列优先级调整。

验收：✅ 635 passed / 12 skipped / 0 failed（+43，零回归）。三个 Tab 均无 `http-equiv="refresh"`；进度由 `activity.js` 就地改写字段（不重建行、不重载文档）；打包工作器现在也发 SSE，完成即推；批量动作对重放安全（重复提交不改变第一次的结果，无法执行的任务连原因一起回报）；停滞种子仍是 `WAITING_TORRENT`、归入「需干预」并保留全部动作，从未被判失败。

> **分组与聚合都算在服务端**：`queue_snapshot()` 一次读出两条队列，分组、计数、需干预聚合与「是否还有东西在动」全在 `app/api/activity.py` 里算完，JSON 端点直接返回它、HTML 页面渲染同一个 dict。分区标题的数字因此不可能和它下面的行数打架，工作台横幅也不可能和队列各说一套。
>
> **`provider='CONVERSION'` 的混淆是真实缺陷，不只是视图问题**：它此前是三条 SQL 里的裸字符串，另外四个来源都有常量名。现在有了 `PROVIDER_CONVERSION`，并**故意不放进 `SUPPORTED_PROVIDERS`**——下载工作器永远不该领走打包任务，而转换工作器只领这一种。
>
> **`live` 决定要不要轮询**：全部可见任务都停在暂停/等待操作者时，页面完全停止发请求；这才是空闲标签页不再每 2 秒唤醒进程的原因。停滞种子仍算 `live`——它坐在「需干预」里，但只有下一次请求才能发现来了做种者。
>
> **顺带修掉的既有缺陷**：`ui.html` 的 `badge` 宏把原始枚举写进 `title`，于是 `WAITING_TORRENT` 出现在悬浮提示和无障碍树里、被屏幕阅读器读出来——正是状态词表要堵的那个漏，只是低了一层；改为 `data-code`。旧页面在一处写「下载完成，正在做种」、另一处什么都不说，做种状态因此新增为**行内注记**（`NOTE_SEEDING`，状态仍是 `COMPLETED`），而不是又一处模板里的中文。`EVENT_LIBRARY` 随书库域一并删除——留着等于让客户端订阅一个永远没有发布者的事件名。

### 阶段 R5：候选域与审核流程（4～5 人日）✅ 已完成

内容：
- `/candidates` 六 Tab（全部/待审核/待补充/已通过/驳回/失败），取代 4 个独立页面。
- 封面网格 + 列表视图切换、排序、多条件筛选侧栏（标签/作者/语言/分类/状态）。
- 批量审核（全选/反选/计数/浮动工具条）、行内快速通过、键盘快捷键（`j/k/a/x`）。
- 元数据抽屉内就地编辑：字段来源标注（gdata/翻译/手动）+ 字段锁定。

验收：✅ 663 passed / 12 skipped / 0 failed（+28，零回归）。六 Tab 均从 `CANDIDATE_TABS` 派生，单词来自 `status.py`；筛选侧栏四维（标签/作者/语言/分类）由 SQL 驱动，每维选值上限 8、`Tags` 多值 AND 关联、单值字段 OR 关联；批量审核通过 `apply_review_batch` 与 JSON API 共享同一协程，逐候选订购验证、对重放安全（已审核的返回 `skipped` 而非报错）；字段锁定后再次刮削不覆盖由 R2 测试覆盖；键盘全流程（`j/k/a/x`）在 `candidates.js` 中驱动真实表单控件，无第二套代码路径。

> **六 Tab 一号模板**：`/candidates` 呈现 待审核（也就是操作员打开页面要干活的队列），全部 列在标签栏最前但作为超集而非默认入口。`_render_candidates` 将整个页面状态放在 `request.query_params` 中，因此一个筛选结果可以通过链接分享、在 Tab 切换时保留。
>
> **筛选侧栏只展示当前 Tab 含有的值，但不对其他已选维度做二次过滤**：前者让操作员知道「这个标签在这个 Tab 里到底有多少」，后者保证一组筛选不会变成死胡同——操作员必须先清除才能扩大范围。
>
> **批量幂等是通过「一次一个候选」实现的**：`approve_and_enqueue` 对收到的 ID 列表是全有或全无的，所以 `apply_review_batch` 每次只传一个 ID 给它——每个候选仍然被独立验证和路由，但一个包含已通过候选的选择不再否决其他 49 个。重复提交只审核仍待审核的，其余通过 `skipped` 回报。审计不受影响——编排器只对实际操作的候选写 `review_actions` 行。
>
> **抽屉只发送变更**：每个字段和锁携带 `data-original`，`changes()` 对它做 diff。如果抽屉把全部 21 个字段都 PATCH 回去，每个字段都会被打上「操作员编辑」标记，从而在下次刮削时压制所有值。
>
> **顺带修掉的既有缺陷**：`candidates.html` 的 `data-legacy="true"` 在此阶段移除，连同 `dashboard.html` 中四个状态指标从中文常量改为 `status.py` 调用的修正。`/candidates/processing` 和 `/works/{id}` 均以 307 而非 301 重定向——浏览器会永久缓存 301，使路径日后无法回收。

### 阶段 R6：统一作品详情页与生命周期（3～4 人日）✅ 已完成

内容：
- `/works/{id}` 作为贯穿候选/下载/入库的统一详情页。
- 状态时间线：节点含时间戳、执行者（自动规则/操作员/系统）、原因、可用动作。
- 底部操作区按阶段切换（候选期：通过/驳回/要求修订/重新排队；下载期：重试/暂停/继续/取消/切源；入库期：重新打包/切源重下）。

验收：✅ 708 passed / 12 skipped / 0 failed（+45，零回归）。候选期、下载期、入库期三种作品都在 `/works/{id}` 打开，只有阶段徽标与底部操作区随之切换；`/candidates/{id}` 及其全部动作的重定向目标一并改向该 URL；时间线把 `review_actions` 与 `download_jobs` 合并为一条倒序列表，能回溯到自动规则命中（`命中规则「…」`）与操作员动作（含其留言），每个任务节点就地提供重试/暂停/继续/取消。

> **入库期的「改路径」和「删除」不在这个页面上**：R6 当时按范围收窄（2026-08-26）把两者划给了下游工具；2026-08-28 恢复后它们落在 `/downloaded`（R10），而不是补回 `/works/{id}`。理由与 R6 自己立的规矩一致——**`/works/{id}` 不新增写路由**：动作仍 POST 到既有端点，一件作品只有一处详情页，一个动作只有一条路径。逐本改名是列表页上的操作，跨作品的路径规则则是 R8 的归档路径模板。
>
> **阶段来自事实而非状态列**：`work_stage` 先看有没有打好包的 CBZ 产物（`artifact_cbz_path`），再看候选是否仍可审核，最后才落到下载期。打包不改候选状态，所以产物是唯一敢说「这本已经入库」的东西；反过来，已入库的作品即使又有一个任务在跑也仍显示入库期——操作员来这个页面是为了那本书。
>
> **一个 URL，但没有第二套写入路径**：动作仍然 POST 到 `/candidates/{id}/…`，R6 只改了它们的重定向目标。新增 `/works/{id}/approve` 会造出第二条通过路径，正是 R5 避开的那件事。`/candidates/{id}` 本身以 307 重定向到 `/works/{id}`，路由函数名保留为 `candidate_detail`，因为仍有模板在用 `url_for('candidate_detail')`。
>
> **任务动作靠受校验的 `return_to` 回到本页**：任务路由属于活动域（`/activity/jobs/{id}/…`），但从 `/works/12` 暂停下载的操作员不该被丢到队列页。页面用隐藏字段说明去处，而隐藏字段就是一处开放重定向面——因此 `local_return_to` 只接受根路径：没有 scheme、没有 `//host`（浏览器确实把协议相对地址当作另一个源）、没有反斜杠（某些解析器会把它规范化成斜杠）、没有控制字符。校验不过就退回原本的队列重定向，动作照做。
>
> **被拒绝的动作重新渲染整页**：`_render_review_error` 委托给 `_render_work`，所以一次拒绝返回的是带时间线的同一个页面，而不是第二份会各自漂移的上下文组装。
>
> **附件种类也是词表**：来源消息里的「图片预览」「压缩包」原先是 `candidate_detail.html` 里的一个三元表达式；R6 把它变成 `ATTACHMENT_KIND_STATUS` 与 `attachment_kind_view()`，由 `work_snapshot` 解析后随载荷下发。同理，四个来源按钮的路由映射（`_SOURCE_ROUTES`）放在 `app/api/works.py` 而不是 Jinja 里，页面和 JSON 客户端因此不可能对「从哪个路由取原档」产生分歧。
>
> **四个来源永远都在**：不可用的那个是禁用加提示原因，而不是隐藏——找不到「EH 种子」的操作员无法分辨「画廊没有种子」和「页面坏了」。`torrent_count` 为 NULL 时说「尚未拉取种子信息」，显式的 0 才说「画廊没有可用种子」。
>
> **`work.js` 只改值，不重画节点**：轮询把阶段/状态徽标与各任务节点的状态、原因就地替换；出现从未渲染过的任务或时间线长度变化时改为播报「这本书有新的进展」并给出刷新按钮。与 `activity.js` 同一策略。元数据编辑刻意不进这个脚本——抽屉的 PATCH 已有实现，第三份会是第三种漂移。
>
> **遗留**：`candidate_detail.html` 自此不再被任何路由渲染，但未删除（等一条明确指示）。`downloads.html`／`downloads_history.html` 是更早阶段留下的同类孤儿。

### 阶段 R8：设置域收敛（2～3 人日）

内容：
- `/settings` 七 Tab：连接、来源规则、自动审批、归档、路径、密码库、系统。
- 归档路径模板 `{category}/{artist}/{title}`，保存前预览渲染结果，对非法字符与冲突有处理（落地 v1 遗留项，原属已删除的 R7）。
- 自动审批规则编辑器（参考 autobrr）：条件分组、字段下拉、实时语法校验、**对历史候选试跑并显示命中数与样例**。
- 系统 Tab：主题、密度、并发上限、轮询间隔、时区。

验收：✅ 809 passed / 12 skipped / 0 failed（+101，零回归）。七个分区都在 `/settings/{section}` 打开，旧的五个路径以 307 指向取代它们的 Tab；轮询间隔、并发上限、时区、归档布局模板保存即生效（前三者沿用 provider callable，时区走 `app.state.display_timezone` + `refresh_display_timezone()`）；非法正则与非法模板在保存时以 400 拒绝且一行都不入库；试跑只读——被命中的候选状态、`review_actions`、`auto_approval_rules` 事后都没变；模板可预览，非法字符被替换、值自带的斜杠被折叠、缺失字段落到兜底段。

> **七个 Tab，不是四个**：合并「路径」进「归档」、「密码库」进「系统」会把一个路径穿越形状的设置和一个重编码偏好放在同一页上。每个 Tab 对应操作员一次只为一件事打开的地方。
>
> **分区码是词表，不是模板清单**：`SETTINGS_SECTION_STATUS` 一处同时命名 Tab、URL 段、导航项和 JSON 载荷。全部为 `neutral`——分区是地点不是状态，给它 tone 会暗示里面的设置本身有健康与否。`settings_section_view` 也是 `status.py` 里唯一遇到未知码就抛 `KeyError` 的解析器：其余解析的是已存历史，旧版本写下的行必须仍能渲染；分区码来自操作员敲的 URL，所以 `/settings/nonsense` 是 404，而不是一个标题为操作员错字的页面。
>
> **页面与 API 共用同一份快照**：`settings_snapshot` 经 `_SECTION_BUILDERS` 分派，页面渲染和 `GET /api/v1/settings/{section}` 调的是同一个函数；测试断言七个分区的页面上下文都是 JSON 载荷的超集——页面只多 `csrf_token`、`error`、`notice`。`PUT` 刻意没写：每个写入都是带自己校验与拒绝文案的表单 POST，JSON 写入会让每个分区多出第二道闸。
>
> **布局模板按路径校验，因为它就是路径**：绝对路径、任意位置的 `..`、未知占位符、缺 `{title}` 都拒。渲染时替换文件系统不接受的字符、把值自带的斜杠折叠（否则「上/下卷」会凭空造出一层目录）、逐段截断、缺失字段落到 `未分类`／`未知作者`／`candidate-{id}`。操作员的模板和 ExHentai 的作者名都是拼进路径的不可信输入。
>
> **预览与保存是同一表单上的两个按钮**：`formaction` 把同一个字段送去预览端点或保存端点，所以预览的就是保存的。预览不写任何东西，也从不是闸门——`save_library_template` 会再校验一次，这才是 `..` 进不了库的原因。规则编辑器的试跑/保存同一形状。
>
> **试跑接受条件而不是已存规则**：试跑的意义正是在保存之前知道结果。它扫最近一段有界窗口内的全部状态，报命中数加最多五个标题；`evaluate_rule` 是纯函数，这条路径上每次数据库调用都是读。
>
> **时区按形状校验，不查 `zoneinfo`**：精简容器可能根本没带 tz 数据库，而格式化发生在浏览器——浏览器永远有完整列表。`ui.js` 从 `<meta name="display-timezone">` 读取时区，就地改写每个 `<time>` 的文本而不动 `datetime`（那是机器可读值），所以关掉 JavaScript 也仍是一个完整时间戳。
>
> **主题与密度不存服务端**，尽管计划把它们列在系统 Tab：它们回答的是「这块屏幕看起来怎样」而不是「这个部署怎么运行」，存在服务端会跟着操作员到一块并不合适的屏幕上。Tab 只说明控件在哪里，不再画一对可能和顶栏那对说法不一致的开关。
>
> **置空是恢复默认，不是存一个空值**：对布局模板，这是「恢复默认」和「把每本书放进库根目录」的区别。
>
> **已存模板若不再通过校验则退回扁平命名，而不是让任务失败**：到那一步书已经下载完、没有操作员在看，为一个设置错误拒绝发布是更坏的结果。非法模板的拦截点在设置页，而它过不了那一关。
>
> **每个 POST 都保留原路径**：约六十处调用点和任何被收藏的表单地址都还能用，只有重定向目标改到表单所在的 Tab。退役的 GET 也保留函数名，因为仍有 `url_for` 指着它们。`settings_redirect` 是各保存共用的唯一助手（硬写 303）；六处退役各自内联 307——退役不是保存，而且 307 不像 301 会被缓存，Tab 划分还能再变。
>
> **遗留**：被取代的五个模板（`connections.html`／`sources.html`／`auto_approval_rules.html`／`archive_settings.html`／`change_password.html`）不再被任何路由渲染，但未删除（等一条明确指示），与 `candidate_detail.html`／`downloads.html`／`downloads_history.html` 一并由 R9 收拾。条件分组目前只有一层扁平 AND/OR：`validate_rule_ast` 接受嵌套，手工写入的嵌套规则仍能求值、仍能渲染 DSL，只是编辑器不会拼。

### 阶段 R9：切换、清理与验收（3～4 人日）

内容：
- 新页面设为默认，移除旧模板与旧路由、移除 `meta refresh` 残留。
- 更新 `README.md`、`.env.example`（新增设置项）、`compose.yaml`（如有变更）。
- 全量回归 + 低配（1C512M / 1GB）压测（落地 v1 遗留的阶段 6）。
- 真实凭据 + 真实 qBittorrent 的 `docker compose up` 端到端验收；登记 `local_save_path`。
- 无障碍与移动端人工走查。

验收：✅ 820 passed / 12 skipped / 0 failed（+11，零回归）。`app/main.py` 120 行（< 500）；八个孤立模板与 `app.css` 全部删除，`data-legacy` 不再存在；`EHBot.md` §8 的 12 条中 9 条已满足并各有测试指名，第 7 条随范围收窄删除，另有三项需要真实主机的验收项**明确顺延**（低配压测、真实凭据端到端、人工无障碍/移动走查）——详见 `progress.md` 的「Deferred From R9」。

> **删除阶段的失败形态不是页面崩掉，而是页面照样渲染而它需要的东西已经没了**。因此每个模板删除前都从三个方向证明无人引用：没有 `TemplateResponse` 指名它、没有 `url_for` 指名曾渲染它的处理函数、没有 include/extends 引用它。`app.css` 之所以能 `rm` 而不是小心切割，正是 R3 把它做成独立文件而非 `ui.css` 的一节。
>
> **`main.py` 2960 → 120 行**：拆出的部分去了 `app/wiring.py`（lifespan 与整张服务图）、`app/web/deps.py`（页面层的请求作用域访问器）、`app/web/rendering.py`（模板与重定向助手）、`app/web/settings_view.py`，以及 `app/web/routes/` 下十一个按域命名的路由模块。页面层与 API 层各有一份 `deps` 并非重复：一个把未登录调用者 303 到 `/login`，另一个返回 401，因为 `fetch` 会静默跟随重定向、把登录页当 200 交回去。
>
> **两条只有浏览器解析器才拦得住的规则，由 `tests/integration/markup.py` 锁住**。其一：HTML 不允许表单嵌套——嵌在另一个表单里的 `<form>` 开始标签会被**忽略**，其子节点连同提交按钮并入外层表单。R9 就是在这里发现 `/activity` 每一行的操作按钮从来没生效过：它们都提交到 `/activity/jobs/batch` 且不带 `action`，页面全程 200。改用按钮上的 `formaction`（`candidates.html` 早已为同一原因记下这个写法）。`<template>` 内的内容豁免，这正是被 teleport 的对话框表单合法的原因。其二：`x-teleport="body"` 会把对话框搬出它所在的表单，所以其中的提交按钮要靠 HTML 的 `form="<id>"` 重新接回——这就是 `ui.confirm` 新增 `form`/`field`/`value`/`formaction` 参数的由来。
>
> **二次确认只给破坏性与花钱的动作**：取消任务（单行与批量）、驳回候选（单件与批量）、切换停滞种子的来源、ExHentai Archive Download、断开 Telegram/ExHentai 连接、删除密码条目。重试、继续、暂停、通过、停止做种仍是一次点击——给每个按钮都加确认，等于教操作员不读就关掉。
>
> **骨架屏由脚本撤下，不靠动画自己过期**：`.ui-card-cover` 在有图可等时带 `data-pending="true"`，`ui.js` 在图片 `load` 或 `error` 时摘掉。CSS 看不到图片的加载状态，而 `loading="lazy"` 意味着请求可能要等到卡片滚进视口才发出；`load` 不冒泡，所以监听器必须在 document 上**捕获**。
>
> **`compose.yaml` 有两处是真漏而非笔误**：`THUMBNAILS_ENABLED` 与 `APP_SECRET_KEY` 都被 `Settings.from_env()` 读取、也都写在 `.env.example` 里，却从未传进容器——设了等于没设。现已透传，密钥默认为空：`resolve_session_secret` 首次启动会自己生成并存下，只在确有配置值时才采用。
>
> **本仓库没有 79 列限制**：动手改行宽之前实测过——已提交的应用代码里有 543 行 ASCII 超过 79 列，最长 152 列。没有这条规则，也没有为它改动任何代码。

### 阶段 R10：已下载域（恢复 §1.3.1）（2～3 人日）✅ 已完成

> 2026-08-28，依操作者指示恢复。触发点是一个具体缺口：已完成的作品无法勾选、无法批量重新打包。
> 恢复范围严格限于 `EHBot.md` §1.3.1，不是把删掉的书库搬回来——详见 R2 那条「部分恢复」。

内容：
- 迁移 `014_downloaded_works.sql`：`artifacts.library_relative_path`（改名/改路径后重新打包的落点）、`removed_works`（移除审计）。
- `Database.list_downloaded_works` / `downloaded_work_counts` / `downloaded_work`：一条 join 同时带上下载产物与打包任务，五个分区共用它。
- `ArchivedWorkService`（`app/downloads/archived.py`）：移除（可选删文件）、重新下载（可选重新打包）、改名/改路径，每个动作都先过路径归属校验。
- `GET /api/v1/downloaded` + `POST /api/v1/downloaded/batch`，与页面读同一个 `downloaded_snapshot`。
- `/downloaded` 五个分区页：网格/列表两种呈现、搜索与排序、勾选 + 浮动批量工具条、单件改名抽屉。
- 状态词表新增 `DOWNLOADED_TAB_STATUS` 与 `DOWNLOADED_PACK_STATUS`，两者都在 `/ui-kit` 里出现。

验收：✅ 939 passed / 0 failed（+73，零回归；无 7-Zip 工具链的主机为 927 passed / 12 skipped）。五个分区各自只显示它命名的那批作品，计数与列表同源；网格与列表是同一个表单的两种渲染；批量三动作逐件执行、可重放，跳过项带原因；移除记录后文件仍在、移除并删文件后两个文件都不在，审计行如实记下是哪一种；改名后文件真的移动、旧目录被清理、重新打包落回新路径；`../../` 与 `..` 在改名与打包两条路上都进不了库外。

> **修掉一个静默失效的既有缺陷**：`_enqueue_sync` 过去只允许 `WAITING_VOLUMES`/`WAITING_PASSWORD` 重置为 PENDING，于是**对已打包作品点「重新打包」是空操作**——`ON CONFLICT DO NOTHING` 之后那行仍是 COMPLETED，worker 只认领 PENDING，页面却 303 回来报告成功。现在 FAILED 与 COMPLETED 也可重排，RUNNING 除外（worker 正持有那一行）。落回同一个文件本来就已处理：`_existing_cbz_paths_sync` 把本书自己的路径列为保留，所以不会长出 `book (2).cbz`。
>
> **改名之后，重新打包必须落回操作员选的位置**。所以路径记在产物行上（`artifacts.library_relative_path`），`_library_target` 先读它、读不到才渲染模板。少了这一步，改过名的书会被搬回模板位置，而且 `unique_library_target` 会把操作员那份当成别人的书、在旁边生出 ` (2)`——一次操作同时表现为「改名被撤销」和「多了一个副本」。读回来的相对路径仍要再验一次（绝对路径、`..` 一律忽略并回落模板）：写入时已经清洗过，但把数据库里的路径拼到根目录上，正是不能只信一次的形状。
>
> **`live` 不能走 `is_live()`**：打包徽章是**派生**词表（由「有没有 CBZ、有没有打包任务、任务什么状态」推出），故意不在 `_REGISTRIES` 里，于是 `is_live("packing")` 返回 False，页面会永远停在「打包中」而不再轮询。快照直接读解析好的 `pack.live`，徽章与轮询决定因此同源。
>
> **「删记录」与「删文件」是两个按钮、两个对话框**，不是一个带复选框的对话框：`ui.confirm` 被 `x-teleport` 搬出表单后只能带一对 `name`/`value`，复选框要手工接回、还可能停在任一状态；而「默认不移除文件」的意义正在于安全的那个是默认发生的。删文件因此是**另一个动作名**（`remove-files`），没点它的请求删不掉任何东西。
>
> **移除留审计行而不是静悄悄删掉**：`list_history_jobs` 一直把终态任务行当作永久记录，直接删会让历史对自己的完整性说谎。行不能加在 `download_jobs` 上——被删的就是它。
>
> **单件查询故意不过滤 `state = COMPLETED`**（列表过滤）。正在重新下载的作品有上一轮的产物、任务行又回到 PENDING；若在这里也过滤，调用方那句「已有任务在进行」就永远走不到，操作员会被告知「该作品没有已下载的档案」——既不对也没法照着做。

## 4. 工作量估算

| 阶段 | 内容 | 估算 | 状态 |
|------|------|------|------|
| R0 | 基线与脚手架 | 1～2 人日 | ✅ 已完成 |
| R1 | JSON API 与共享审核编排 | 4～5 人日 | ✅ 已完成 |
| R2 | 缩略图与数据模型增量 | 2～3 人日 | ✅ 已完成 |
| R3 | 设计系统与通用组件 | 3～4 人日 | ✅ 已完成 |
| R4 | 活动域 | 3～4 人日 | ✅ 已完成 |
| R5 | 候选域与审核流程 | 4～5 人日 | ✅ 已完成 |
| R6 | 统一作品详情页 | 3～4 人日 | ✅ 已完成 |
| ~~R7~~ | ~~书库域~~ | — | 已删除（2026-08-26）；2026-08-28 以更窄范围恢复为 R10 |
| R8 | 设置域收敛（含归档路径模板） | 2～3 人日 | ✅ 已完成 |
| R9 | 切换、清理与验收（含 `main.py` 拆分收口） | 3～4 人日 | ✅ 已完成（三项主机相关验收顺延） |
| R10 | 已下载域（恢复 §1.3.1） | 2～3 人日 | ✅ 已完成 |
| **合计** | | **27～37 人日** | |

建议交付顺序：**R0 → R1 → R2 → R3** 为地基（无地基则后续页面无法脱离整页刷新），随后 **R4 → R5** 交付最高频的日常操作面，**R6** 补齐全链路视图，**R8 → R9** 收口，**R10** 补上已下载内容的批量操作面。R4 完成即可让用户感知到最大的体验跃迁。R10 是原 R7 的窄版：清单 + 批量三动作 + 单件改名，阅读器与书架仍交给下游工具。

## 5. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| `main.py` 拆分引入回归 | 现有功能损坏 | 拆分前锁定回归测试；按域小步迁移，每步跑全量测试 |
| HTMX 局部更新与 CSRF 冲突 | 写操作失败 | 统一在 HTMX 请求头注入 CSRF；保留同源校验；补集成测试 |
| SSE 在反代下被缓冲 | 实时性失效 | 文档说明反代配置（禁用缓冲）；SSE 失败自动降级为纯轮询 |
| 第三方图床防盗链/失效 | 缩略图缺失 | 服务端代理 + 缓存 + 占位图回退；不缓存失败结果过久 |
| 缩略图占用磁盘 | 空间耗尽 | 尺寸上限 + LRU 清理 + 就绪检查纳入空间告警 |
| 新旧页面并存期状态不一致 | 操作困惑 | 过渡期用特性开关，同一时间只暴露一套给用户 |
| 低配环境前端资源偏重 | 首屏变慢 | HTMX+Alpine 合计约 50KB；静态资源长缓存 + gzip；不引入构建链 |
| 范围蔓延 | 工期失控 | 严格执行 `EHBot.md` §6.2；新功能请求另立需求 |
| 缩略图代理被当成开放代理 | 成为他人的图片中转 | 接口只接受 hash；唯一准入口是刮削时写入的 `thumbnails` 行 |
| 已下载域会删真文件 | 误删不可恢复 | 默认只删记录，删文件是另一个动作名 + 另一个确认框；每个路径先证明落在库内/工作目录内（含符号链接解析后）；越界只报告不删；留 `removed_works` 审计 |
| 恢复的域重新长成书库 | 范围再次失控 | R10 的边界写进 `EHBot.md` §4.5 与 `AGENTS.md`：不做阅读器、书架、导入扫描、首图封面；详情页仍只有 `/works/{id}` 一处 |

## 6. 测试策略

| 层次 | 覆盖 |
|------|------|
| 单元 | API 序列化/分页/错误体、缩略图生成与魔数校验、状态徽章映射、路径模板渲染与非法字符 |
| 集成 | 每个 API 的读写路径、批量动作幂等、SSE 事件在状态跃迁时发出、CSRF 生效 |
| 回归 | 既有全绿（R10 基线 939 passed；无 7-Zip 工具链的主机 927 passed / 12 skipped）；来源降级链、归档流水线、安全门禁、幂等契约行为不变 |
| 端到端 | 真实凭据 + 真实 qBittorrent 的完整链路；Docker 内 7-Zip 托管安装 |
| 非功能 | 1C512M / 1GB 压测；断网、重启、磁盘不足；无障碍与移动端走查 |

## 7. 完成标准

对应 `EHBot.md` §8 的 12 条验收标准，全部满足即视为重构完成。核心三条：

1. 全站无整页刷新，进度经轮询 + SSE 局部更新。
2. 顶级导航以 Tab 聚合候选/活动/已下载/设置，封面驱动的主视图。（`EHBot.md` 写的是 5 域；第 4 域于 2026-08-28 以 `/downloaded` 落地，五域齐备。）
3. `main.py` < 500 行，API 与页面分层，测试全绿（R3 基线 592 passed / 12 skipped）。

## 8. 与 v1 计划的关系

v1（`DEVELOPMENT_PLAN.md` 1.0）是从零建设方案，其阶段 1～5 已实现并交付，阶段 6（低配优化与发布）未执行。本 v2 计划：

- **继承**：v1 已建成的后端架构、数据模型、安全与幂等契约。
- **取代**：v1 的 UI 与流程控制部分（原阶段 2～5 中的 Web 审核界面设计）。
- **吸收**：v1 遗留的阶段 6 低配验收、`{category}/{artist}/{title}` 布局，分别并入 R9 与 R8。
- **保留待办**：BRIDGE profile 协议、加密 RAR fixture 仍为独立待办，不在本次重构范围。